export interface IResultProduction {
  dates: Array<string>;
  total: Array<number>;
  year: string;
}

export interface IProductionOilByState {
  area: string;
  total: number;
  product: string;
  description: string;
  units: string;
}
